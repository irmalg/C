#include <stdio.h> //Librerias

int main() //Main
{
   int n; //Declaracion de variable
   n = 0; //Asignacion de variable
   	while (n < 0){ //Ciclo repetitivo While
	      printf( "\tn vale: %d\n", n );
	      n++; //Incremento n
    }
    printf( "\nFin de while\n", n );
   return 0;
}
